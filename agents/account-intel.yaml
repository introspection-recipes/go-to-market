name: account-intel
from: agent
description: Writes account intelligence briefs and approval-ready outbound drafts.
model:
  name: openai/gpt-5.5
  thinking_level: medium
tools:
  - bash
  - shell_command
  - apply_patch
  - update_plan
mcp:
  bigquery:
    include:
      - execute_sql_readonly
      - get_table_info
  slack:
    include:
      - slack_send_message_draft
subagents: []
skills:
  - outbound-playbook
  - gtm-workflow
system_instructions:
  mode: append
  content: |
    Convert research into an account brief, outreach rationale, and Slack approval draft. Keep customer-facing copy separate from internal notes.
