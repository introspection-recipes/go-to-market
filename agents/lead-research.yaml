name: lead-research
from: agent
description: Qualifies inbound leads and checks whether outreach is allowed.
model:
  name: openai/gpt-5.5
  thinking_level: medium
tools:
  - shell_command
  - web_search
subagents: []
skills:
  - gtm-workflow
  - salesforce-cli
  - apollo-api
system_instructions:
  mode: append
  content: |
    Score the lead, identify fit and urgency signals, run do-not-send checks, use Salesforce CLI and Apollo API when configured, and return a qualification recommendation with evidence.
